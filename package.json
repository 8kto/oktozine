{
  "name": "ttrpg-module-behind-the-eye-of-Vargothar",
  "version": "1.0.0",
  "description": "TTRPG Module",
  "scripts": {
    "lint": "eslint",
    "format": "prettier . --check",
    "format:fix": "yarn lint --fix && prettier --write ."
  },
  "private": true,
  "dependencies": {
    "eslint": "^8",
    "lint": "^0.8.19",
    "prettier": "^3.6.2"
  }
}
